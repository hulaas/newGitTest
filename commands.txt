git clone url
git config --global user.email "youremail@hotmail.com"


1. git pull
1. git status
2. git add "filename.txt" (spesifikk fil)
2. git add . (alle filer)
2. git add -A (alle filer)
3. git commit -m "message"
4. git push

$ git checkout -b [name_of_your_new_branch]
#hvis commit uten -m vil man komme inn i VI text editor, trykk esc så ":wq" enter.
#git checkout "branchname" for å bytte branch
#git branch for å se branches
#git merge "master" for å merge branches

#Ravndal har vært å lagt til en linje :)
